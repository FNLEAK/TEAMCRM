# Copy to .env in project root and server/.env (or symlink)
DATABASE_URL="postgresql://postgres:postgres@localhost:5432/pipeline_crm?schema=public"
JWT_SECRET="change-me-to-a-long-random-string-in-production"
PORT=4000
